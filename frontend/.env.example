# Copy this file to .env and fill in real values. Never commit the real .env.

# Leave empty for local dev — Vite's proxy (vite.config.js) forwards /api
# calls to localhost:5000. In Vercel's project settings, set this to your
# deployed Render backend URL, e.g. https://your-api.onrender.com
VITE_API_URL=
